#include <cstdio>

using namespace std;

typedef long long int lli;

lli fastExpo(lli b, lli e, lli m) {
    /*
     * Perfroms the fast exponentiation for b ^ e mod m.
    */
    
    // CODE COMES HERE
    
    return 0;
}

int main() {

    lli b, e, m;

    scanf(" %lli %lli %lli", &b, &e, &m);
    
    printf("%lli", fastExpo(b, e, m));

    return 0;
}
